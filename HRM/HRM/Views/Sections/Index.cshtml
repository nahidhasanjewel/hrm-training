@model IEnumerable<HRM.Models.Section>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Depts.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SectionCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SectionName)
        </th>

   
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Depts.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SectionCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SectionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
